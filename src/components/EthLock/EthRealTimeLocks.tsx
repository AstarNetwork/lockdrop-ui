import React from 'react';
import SectionCard from '../SectionCard';

const EthRealTimeLock = () => {
    return (
        <div>
            <SectionCard maxWidth="lg">
                <h4>Lock Claims</h4>
            </SectionCard>
        </div>
    );
};

export default EthRealTimeLock;
